on: [push]
jobs:
  jmeter_job:
    runs-on: self-hosted
    name: JMeter Test Execution
    steps:      
    - name: Checkout
      uses: actions/checkout@v3
    
    - name: Run Zebra Tests
      uses: ./
      with:
        test-plan-path: ./Platform_Prod_US_v2
      
        
  
